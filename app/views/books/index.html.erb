<h1>Books</h1>
<p><%= link_to 'New Book', new_book_path %></p>
<ul>
  <% @books.each do |b| %>
    <li>
      <%= link_to b.title, b %>
      <% if b.authors.any? %> — <%= b.authors.map(&:name).join(", ") %><% end %>
    </li>
  <% end %>
</ul>
