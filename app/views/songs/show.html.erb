<h1><%= @song.title.presence || "Untitled" %></h1>

<p>
  Singers:
  <% if @song.singers.any? %>
    <%= @song.singers.map(&:name).join(", ") %>
  <% else %>
    —
  <% end %>
</p>

<p>Released on: <%= @song.released_on.presence || "—" %></p>

<p>
  <% if @song.youtube_url.present? %>
    <%= link_to "YouTube", @song.youtube_url, target: "_blank", rel: "noopener" %>
  <% end %>
  <% if @song.spotify_url.present? %>
    <% if @song.youtube_url.present? %> | <% end %>
    <%= link_to "Spotify", @song.spotify_url, target: "_blank", rel: "noopener" %>
  <% end %>
</p>

<p>
  <%= link_to "Edit", edit_song_path(@song) %> |
  <%= link_to "Back to Songs", songs_path %>
</p>
